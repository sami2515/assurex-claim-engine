services:
  - type: web
    name: assurex-claim-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
